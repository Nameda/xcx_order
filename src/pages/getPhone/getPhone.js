var regeneratorRuntime = require("../../lib/runtime.js");//index.js
var app = getApp();
Page({
    /**
     * 页面的初始数据
     */
    data: {
    },
    getPhoneNumber: function(e){
        console.log(e);
        // var a = this.data.menuitems;
        // a[1].text = e.detail.encryptedData.purePhoneNumber;
        // this.setData({
        //     menuitems:a
        // })
    }
});